pre-commit:
  parallel: false
  commands:
    fmt:
      run: pnpm fmt
      stage_fixed: true
    lint:
      run: pnpm lint

pre-push:
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm test
