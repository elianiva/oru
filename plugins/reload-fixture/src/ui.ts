/**
 * The reload suite's UI facet: one claim the host joins to the bundle it
 * builds from this file. Tests append a probe export to change the bytes.
 */
export const defs = [{ slot: 'composer', defId: 'widget' }]
